[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lda-pure-humanize"
version = "0.2.2"
description = "E2B-native Pure Humanize flow for Ubuntu package optimization"
requires-python = ">=3.12,<3.13"
dependencies = [
  "e2b==2.45.0",
  "openai-codex==0.147.0; sys_platform == 'linux'",
  "pydantic==2.12.5",
  "PyYAML==6.0.2",
]

[project.optional-dependencies]
test = [
  "pytest==8.4.1",
  "pytest-asyncio==1.1.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
lda = "lda.cli.main:main"
lda-tool-gateway = "lda.gateway.server:main"
lda-mcp = "lda.gateway.mcp:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
