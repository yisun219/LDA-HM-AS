# 任务卡:<卡名>

## 目标(一句话)

<优化哪个库/组件,做成什么样算完>

## 兼容红线(必须原文保留)

本任务的一切改动必须守住流程文档 §3 的八条兼容边界;
任何接口或可观察行为的改变,无论提速多少,一律不收。

## 背景与已知事实

<相关的共享事实、别人踩过的坑,先抄进来>

## 检查点定义(与 state/GATES.tsv 一一对应)

- **G0 前提探针**:用上游源码/构建日志证明要改的旋钮在原版确实没开;不成立即弃点。
- **G1 <名称>**:判据 <优先用产物:哈希/计数/内容,而非时间>;
  证据要求 <五要素>;有效性验证 <对什么故意做坏的样本必须报错>。
- **G2 micro benchmark**:输入集 <列出>;判据 <各输入无回退,总体提速>。
- **G3 兼容检查**:符号表比对 + abidiff + 真实调用方冒烟,全部零差异。
- **G4 end-to-end**:<真实应用场景>,三段复测(A→B→A′)配对测量。

## 本卡适用的测量与环境规则(从流程文档 §9/§13 摘)

- 三段复测,漂移作废;重复次数先算;不同机器/作业的数字不相减;
- 关键步骤各带退出码与产物断言,作业整体 rc=0 不等于每步成功;
- 在 E2B 沙箱内执行;A/B 对比在同一实例内配对;
- 临时文件只写沙箱内或指定暂存区,收尾"残留=0"复核;
- 结论立刻落盘。

## 审查反馈(只增不删)

<!-- 检查 agent 追加;优化 agent 不得修改或删除已有条目 -->
