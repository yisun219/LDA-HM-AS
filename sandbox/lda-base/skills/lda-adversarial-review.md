# LDA Adversarial Review

The Builder and Reviewer must be separate sessions. The Reviewer independently
recomputes results, checks job state and evidence hashes, runs baseline tests,
constructs negative samples for each fence, and searches the trace for bypasses,
post-hoc criterion changes, cherry-picking, and reward hacking.
