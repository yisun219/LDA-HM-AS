# Intel Performance Skills

This template targets `INTEL(R) XEON(R) GOLD 6548Y+` when an optimization is
architecture-specific. The upstream Intel skill set should be installed into
this directory from `https://github.com/intel/intel-performance-skills` and its
commit pinned in the task card before use. Do not silently use host-specific
instructions in a portable card.
