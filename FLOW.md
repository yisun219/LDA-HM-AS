# Linux Development Agent 开发流程

> Linux Development Agent(LDA)的开发流程规范全文;项目概览与快速上手见 README.md。
> 流程的修改通过对本仓库提 PR 完成。基座致谢见文末。

## 第一部分:运行时基座 —— 四角色 autoresearch 循环

### §1 四角色循环与持久状态

运行时用四个角色在**持久项目状态**上执行边界清晰的任务:**Manager → Planner → Engineer ⇄ Reviewer**。

| 角色 | 职责 | 在本项目部署里的对应 |
|---|---|---|
| **Manager** | 解释操作者意图、选择工作流、拥有阶段转换权限 | 编排会话(+操作者本人) |
| **Planner** | 选择下一个高价值任务,并定义它必须产生的证据 | 选包与开卡(改造见 §6) |
| **Engineer** | 实施、研究、跑实验、产出可检查的产物 | 优化会话(改造见 §4) |
| **Reviewer** | 独立检查正确性、证据、限制与完成情况 | 检查会话(改造见 §5) |

基座的三条性质:

- **执行与验证分离,常规轮次以 Reviewer 的判定收尾**;
- **持久状态**:任务、检查点、决策、技能、证据跨会话/跨升级保留——本项目的落地载体
  是任务卡(每卡一个 git 仓,§2);工作可在任何阶段停止,从最后验证过的位置恢复;
- **操作者升级点**:关键决策保留给人(本项目的落地=§7"人改 flow")。
  新的知识、技能、验证器要进入框架,必须先过角色审查与任务级验证
  (本项目的落地=每个检查先对已知坏样本报错,§10)。

运行入口:仓库根的 `./lda`(doctor / new / run / status / review);循环内 agent 全自主推卡,人只在操作者升级点介入;**引擎后端可换**(Claude Code、Pi、Codex CLI 等),换引擎不换协议;
**接入方式也不限于 API**:引擎既可以走带 API key 的推理服务,也可以用已登录的订阅版 CLI
(例如订阅版 Claude Code 直接跑 Opus 级模型,不需要任何 key)——两种接入本项目都实际跑通过,
换接入方式同样不换协议。

### §2 任务卡与 autoresearch 循环

持久状态在本项目的载体是**任务卡**——agent 在一张卡上自主迭代直到收敛:

1. **开卡**:任务书(`prompt.md`)、检查点清单(`GATES.tsv`)、唯一进度指标
   (`measure.sh`,输出 `METRIC gates_passed=N`)、防作弊核验(`checks.sh`)、
   迭代上限(`config.json` 的 `maxIterations`)——五件套先定死,再开工;
2. **每一轮**:agent 读任务书与当前门状态 → 推进未通过的检查点(下文简称"门";改代码/跑实验/写证据),
   micro benchmark 的即时结果就是它这一轮的即时反馈(reward) → 每步 git 提交;
3. **轮末**:框架跑 `measure.sh` 取进度分、跑 `checks.sh` 查作弊;
4. **循环**:没到目标且没触迭代上限 → 下一轮;全部检查点到达终态(或达到上限)→ 收卡;
5. **收敛的定义**:不是"分数变高",而是 §3 的兼容红线全过、且每个"通过"挂着真证据——
   分数只是循环的燃料,红线才是出口。

卡的骨架见 `templates/task-card/`,即拷即用:

- **任务书**:目标 + 兼容红线 + 检查点定义;审查反馈**只增不删**;
- **检查点清单**(`GATES.tsv`):每项的状态与证据文件路径;
- **防作弊**:每个"通过"必须挂一份自含五要素的证据文件——
  ①执行的命令 ②原始输出关键行 ③作业编号或产物路径 ④为什么支撑结论
  ⑤**作业在调度系统里的最终状态与退出码**(实测教训:有作业被抢占仍产出了数据,
  终态必须披露并单独裁决,不能默认"跑完=干净")。
  Reviewer 会按编号回查原始产物;**造假 = 整卡清零**。
  防作弊检查的最低覆盖:证据文件存在且非空、作业号真实可查(格式合法的伪造号要能识破)、
  被引用的原始数据在卡内且未被篡改(哈希登记)——这三类是实测中用伪造证据做压力测试时漏网过的盲区;
  **哈希登记必须覆盖门证据文件本身,不只原始数据**——只登记原始数据、漏登门证据文件时,多个门的证据文件
  可被整体掉包而防作弊检查零反应(实测漏网);
  **门号映射必须机械自检(双向)**:每个门都有对应检查、每个检查都指向真实存在的门,
  由脚本自己校验而不是靠人核对(实测:往伪造证据里塞一句检查要找的关键词就能骗过
  纯关键词匹配——内容核验要认指纹不认口号);
- **证据完备性**:产出数字的脚本必须在对应检查点判为通过**之前**提交入卡;
  每个被引用的统计量,背后的原始数据文件必须留在卡里(收尾清理**不得删除**被证据
  引用的原始文件——清理的是暂存,不是证据);置信区间要写明公式与自由度
  (小样本用 t 不用 z,**分布与自由度口径在卡内钉死、文本与实现必须同一口径**——
  实测出过卡面写 t、脚本用 z,区间窄了两三成);**原始数据必须含逐次观测值**,
  只存汇总统计不算原始数据——第三方要能从原始文件重算出置信区间;
  证据里引用的文档必须真实存在于卡内;
- **判据单一来源**:预注册(在看到任何数据之前写定,写定后不许改)的判定规则必须以可执行断言的形式存放在一处,
  脚本运行时回读校验——文本与实现不一致即失败(实测教训:两处规则靠人肉比对,
  差点让"少跑一步"成为定案);
- 没有调度作业的检查点,证据第⑤要素显式标 `NO-JOB`,并由防作弊检查强制;
- **防作弊检查的门号映射是卡级配置**,开卡时逐卡核对(从别的卡拷来的映射会检不存在的门、
  漏检真实的门,实测发生过且一轮没被发现);**收尾必须运行防作弊检查并把结果写进证据**;
- **一卡一部件,严格隔离(并行的前提)**:每张卡只改一个部件(及其包族——同一源码包产出的全部二进制包),
  改动只存在于本卡自己的源码树与沙箱里;**禁止跨卡共享工作副本或中间产物**,
  基线一律只读引用并用哈希锁定;每卡独立打包出自己的 .deb。
  多个优化的**组合效应不在单卡里测**——由单独的集成卡把各卡产出的包组合安装后
  统一测端到端;单卡不得夹带任何他卡的改动,发现夹带按造假处理;
- **一卡一线**:同一张卡同一时刻只允许一条 Engineer 循环在驱动;循环驱动器必须带
  卡级互斥锁,第二个实例启动即拒绝;会话或终端重启后,先清点存活的驱动实例与仍在运行的
  引擎会话,再决定接管还是重启——孤儿循环(发起它的会话已关、循环仍在跑)不会随终端会话消失(实测:两条线同写一张卡,
  互相覆盖对方已提交的证据文件,险些毁掉证据可信性);
- 每一步 git 提交,改动全程可回溯。

---

## 第二部分:针对本场景的定制改造

> 基座提供通用的循环骨架;下面这些是本场景的完成标准、工具、测量与证据要求——LDA 的主体设计。

### §3 完成标准的改造:ABI / FFI 兼容 —— 最核心、最硬的边界

产品形态先说死:交付物是**软件包(.deb)**,不是系统镜像(镜像只当内部集成测试环境);
**手术刀式替换**的定义:装上我们的版本后,**已有二进制不用重编译、应用不用改、
开发者代码不用改、编译链接方式不用改**——一切照旧,只是更快。

这条是排第一的约束,高于任何性能收益,**加速不能以改变任何接口或行为为代价**;
它同时是**整个 flow 的收敛标准**:一轮优化什么时候算"做完",不看提速了多少,
看兼容检查是否全绿——**性能是目标函数,ABI/FFI 兼容是收敛条件**。
Engineer 与 Reviewer 的循环只有两种出口:兼容全过(收敛,进入端到端验证与交付),
或者放弃这个优化点;**不存在"快了很多但兼容差一点"的第三种出口**。

一个优化过的包要进仓,八条线全部守住,每条有自动检查:

| 边界 | 不许变的东西 | 怎么检查 |
|---|---|---|
| 二进制 ABI | 库文件名(SONAME)、导出符号零删改、公开结构体布局、符号版本 | 符号表逐一比对 + `abidiff` 类型级差分 |
| FFI(外部调用面) | 其他语言绑定看到的接口与行为(Python 绑定、PAM 栈等) | 原版系统上编译的调用程序,不重编译直接在优化版上跑,结果一致 |
| 行为 | 命令行参数、输出格式、退出码、数据文件格式 | 同输入两版输出逐字节比对(允许白名单化的已知无害差异) |
| 配置 | 用户既有配置在安装/升级/卸载后完好 | 安装→升级→卸载→重装全循环 + 完整性校验 |
| 硬件覆盖 | 不让任何原本正常的机器出问题 | 多种硬件/虚拟机拓扑真实开机验证 |
| 系统升级 | Ubuntu 官方安全更新照常到达 | 版本号规则检查(§12)+ 真实执行一次系统升级 |
| 安全默认值 | 安全缓解、沙箱、签名验证默认不弱化 | 取舍类改动只做 opt-in 并把代价写进包里 |
| 结果等价 | 任务产物(装出的文件、生成内容)不变 | 产物逐字节比对 |

**裁决**:任何一条被真实破坏 = 一票否决,不讨论收益多大。
同样重要:**"没测出问题"≠"证明了兼容"**——每条检查启用前,
先拿一个故意做坏的样本证明它能报错;抓不住坏样本的检查不算数。
八条线并非每条都适用于每个改动(纯配置的改动没有二进制接口)。
**不适用的线必须显式标"不适用"并写一句理由,不许静默跳过**——空白和"不适用"是两回事。

### §4 Engineer 的改造:两层 Benchmark,micro 作即时反馈

**Micro benchmark(微观)——Engineer 的即时反馈。**
对每个被优化的库/函数生成多种代表性输入(不同尺寸、分布、边界情况);
全自动、秒级,每次迭代都跑,作为即时反馈;任何一种输入变慢都要解释。

**End-to-End benchmark(端到端)——验证系统级折射。**
用真实应用验证:Chrome 页面渲染、桌面 GUI、Web server 吞吐、系统启动、安装/升级软件包。
回答:**多个库的微观优化,最终折射成用户可感的系统级提速了吗?**
端到端负载开测前先做**一次不计成绩的试跑(干跑),标定**每轮耗时;实测与预估偏差大(超 3 倍)就先重设计负载
再进正式复测——负载耗时估错过 10 倍,整段测量直接报废。
实测教训:微观提速折射到端到端,比例出现过 98%,也出现过 35%——
取决于下游是否真在等这条路径,所以微观数字永远不能代替端到端数字。
反向同样成立:**端到端的通过不能自动消解 micro 层的回退否决**——只有当端到端负载
确实覆盖了回退的输入类,它才构成解释;覆盖不到,否决条款仍然有效
(实测先例:git 负载不含零填充输入,不能为全零字节输入上 −14.5% 的回退开脱)。

**呈现规则**:两组结果都正式入账、各自标层。micro 记录每个库/函数的收益与回退,
是复用、审计与解释"钱从哪来"的资产;端到端记录系统级折射。
引用对准层:库级结论引 micro,系统级结论引端到端;两层互不代替、不得并排相加
(这条由防作弊检查做机械校验:同一行/同一表格不得同时出现两层数字)。
(正确性与兼容不算 benchmark,是 §3/§5 的关卡——不过关,快慢无意义。)

**层责任与收敛(部件卡 vs 集成卡)**:部件卡的收敛条件是 §3 的八条兼容线,
而不是"端到端必须显出提速"——micro 有真收益且八条线全绿的部件卡即可交付;端到端在部件卡里是
**记录义务**(如实测、如实标层),不作为该卡的否决闸;多条同链部件加起来的端到端效果由
**集成卡**把各卡产出的包组合安装后在链级统一裁决(单部件折射可以很小,链级叠加才是账)。
两条否决条款不受此影响:micro 层的净回退照样一票否决;micro 无收益(诚实零)则无物可交付。

### §5 Reviewer 的改造:对抗验证

Reviewer 角色在本场景被具体化为**对抗验证**:
**性能绝不能以改变功能为代价**,保证方式是角色对抗,而且内建在框架里,不是外挂步骤:

- Engineer(优化)与 Reviewer(检查)**必须是不同会话**;
- Reviewer 专职攻击:跑 §3 全部兼容检查、构造刁钻输入找行为差异、
  复核测速方法有无作弊空间、按作业编号回查证据;
- 结论三档:**确认通过 / 证据不足打回重做 / 发现造假整卡清零**;
- 检查器自己先受检:每个检查项先对故意做坏的样本报一次错才算有效。

### §6 Planner 的改造:选包按优先级,不做全量

Ubuntu 有几万个包,**绝不从头挨个优化**。Planner 先按三个维度筛出一小批:

1. **使用频率**:默认安装集、装机量统计、常驻进程与高频调用路径;
2. **性能关键度**:真实场景热路径(页面渲染、文件 IO、压缩、加解密、启动、包管理)上
   profiling 时间占比高的;
3. **依赖图重要度**:反向依赖数多、被大量程序链接的(一次优化惠及面大)。

三维打分排序 → **候选清单**(每期 10–20 个)入库可复查。打分纪律:
①公式与权重在**读任何数据之前**注册并入卡(内容做哈希存证);
②"使用频率"维必须声明参照安装集(桌面/服务器/构建环境会系统性偏置彼此的库);
③反向依赖统计必须处理别名(Provides 声明、改过名的过渡包等——不处理,计数可以差一个数量级);
④性能关键度的证据分三档:实测归因 > 链接关系 > 推断,引用时标档;
  **沙箱化应用(如 snap)自带库,不能为系统库的热路径作证**;
⑤候选清单与打分数据固定入卡的 evidence/,供跨期复查。
目标是**通用系统级提速**——多数用户不跑任何特定任务也受益,不死磕单一 workload。
选题可以**按依赖链组卡**:同一条高频路径(如一次 apt 安装事务、一次桌面会话启动)上的
多个部件各开一卡并行推进——单部件的端到端折射可以很小,链级叠加由集成卡统一测(§4);
组链时仍然一卡一部件,严禁跨卡共享改动。

选定之后、开卡之前,先做**前提探针**:用上游源码或构建日志证明想改的旋钮在原版里
确实没开;前提不成立立即弃点。(实测教训:zlib 的上游打包脚本本来就开着 -O3,
凭想当然会白跑一整轮。)探针结论必须区分**已证段与未证段**,未证段显式列出、
交给 Engineer 开工时复核,复核证伪即当场停卡。
判"某个旗没传"之前,**先确定发行版实际用的是哪套构建系统**——autotools 与 cmake
的默认值可以正好相反,读错入口是探针最容易判反的方式(实测:libpng 差点因此误判)。

### §7 操作者升级点的改造:人一定是改 flow 的,然后让 flow 来做

- 人类协作者的工作对象是**这套流程**:调整优先级标准、增改边界与检查、
  修订测量规则——全部通过对本仓库提 PR 完成;
- agent 的工作对象是**流程之下的任务**:候选清单里的优化、两层 benchmark、
  对抗验证、打包交付;
- 出了问题,第一反应不是人上手修那一处,而是问:**流程里缺了哪条规则/哪个检查,
  让这个问题溜进来了?**把规则补上,让同类问题永远过不来。

### §8 运行底座的改造:E2B Sandbox

E2B 是**为 agent 设计的轻量沙箱容器**(与 Docker 类似,但面向大规模并发的
agent 会话与秒级环境重建;实验室已自建服务)。整个流程统一跑在它里面,不依赖裸机。
它在 flow 里这样用:

- **模板(Template)= 预装好公共依赖的基础环境**:Ubuntu 26.04 + 工具链 +
  两层 benchmark 环境预装一次、存成模板(即快照;新沙箱从快照秒级克隆出来);
  模板不存在就构建一次,之后所有任务复用——避免每张卡重新配环境;
- **沙箱(Sandbox)= 一次性的干净工作间**:每张任务卡(以及卡内每个需要干净环境的
  实验轮)从模板创建自己的沙箱,带超时;构建、测试、micro benchmark、环境恢复、
  agent 会话全部经 Sandbox API 在沙箱内执行;
- **沙箱是默认执行地,箱外要付书面理由**:构建、测试、micro 测量一律进沙箱;
  只有沙箱能力做不到的负载——需 KVM 的虚机台架、整机 GUI 会话、以及 §9 禁止沙箱定量的
  同步等待类测量——允许用调度集群或受控真机,且证据里必须写明"为什么沙箱做不了"这一句;
  没写理由的箱外实验,审查按违规打回;
- **脏了就丢,永远不修**:环境被实验弄脏就销毁、从模板重建——干净环境是廉价品,
  这替代了一切"环境恢复"手工活;
- **持久的东西不放沙箱**:任务卡(git 仓)、证据、账本都在沙箱外持久保存;
  沙箱只提供算力与环境,销毁不丢任何该留的东西;
- **并发实测口径**:同时持有 24 个沙箱各自满载,每箱仅慢约 17%——并行派发(扇出)宽度建议 8–16;
  想再高,先做"同时持有并满载"的实测再放开;
- **长命令必须带心跳**:网关会掐断静默超过约 30 秒的命令流(与命令总时长无关),
  长构建/长测试要周期性输出;管道缓冲(如 `cmd | tail`)也会把命令变成静默,同样中招
  (并行派发驱动已内置心跳);**心跳不等于进度**——心跳只防网关掐流,挂死的命令
  会被心跳伪装成健康,长步骤必须自带进度断言与超时(实测:一箱 apt 挂死 6 分钟,
  心跳全程"正常");
- **取证先于销毁**:证据文件必须在销毁沙箱**之前**取出,且**成败都取**;
  文件出箱做内外双哈希比对;
- **绝不复用脏沙箱**:换一个新沙箱不到一秒,过期沙箱自行消亡——任何"修一修接着用"的
  设计都是错的;
- **沙箱内计时纪律**:单次负载做到 **≥10 秒**才配声称 1% 量级的结论
  (实测:0.4 秒负载只能分辨 ~9.5%,13 秒负载能分辨 ~1%;噪声主体是每次调用的固定开销);
  A/B 必须同一沙箱实例内 **A,B,A,B 交替**配对(不交替会凭空造出约 5% 的"先跑者更慢"),
  并遵守 §9;最终端到端数字需要时在受控物理机/固定虚拟机复测确认;
  **文件同步类等待(fsync/sync_file_range)在沙箱的分层文件系统上近乎免费——
  想消除这类同步等待的优化点,禁止在沙箱内测收益大小,必须在带真实磁盘的受控机器上测**
  (实测:预加载去同步库砍掉 97% 的 fsync,沙箱内 wall 却纹丝不动);
- **循环驱动跑在常驻环境**:autoresearch 的外圈驱动(逐轮调引擎的循环)应部署在
  常驻环境(集群守护会话/云端),不依赖操作者的终端会话——实测:终端会话重启两次
  打断了四条循环(卡状态零丢失,但高强度运转不该系在一台不能关的电脑上);
  引擎会话本身入箱是收敛方向;过渡期引擎会话可跑在常驻环境,但它的一切构建/测试/测量
  必须经 Sandbox API 或受控节点完成(与上一条计时纪律的例外口径一致);
- 网关地址、密钥等**接入配置不写在本文档里**,向管理员索取。

---

## 第三部分:卡上的执行细则

### §9 测量规则(违反即作废)

1. **三段复测**:先测原版(A)→ 优化版(B)→ 回测原版(A′)。
   A 与 A′ 差异明显 = 环境漂移,本轮作废,不许拿来解释;
2. **配对比较**:A/B 同一实例同一作业内交替跑,算逐对差;
   **对照臂之间的测量仪器必须相同**——strace 的有无、过滤器的宽窄,都算不同仪器
   (实测:三臂各带不同仪器,单调下降被误读成环境暖化);
3. **重复次数先算后跑**,判定看"效应比漂移大多少":最终判据是
   **效应量数倍于自身漂移**(漂移=A 与 A′ 之差;可信结论的效应实测普遍在漂移的 9–23 倍),
   重复次数只是达到这个判定力的手段;可靠检出 5% 差异约需 59 次重复,
   "跑 8 次没坏"对小概率问题几乎无检出力;置信区间过宽时,"没看见回退"只能记
   "无法判断",不得当作"证明无回退";固定的绝对阈值(如 ±2%)应与该读数的
   实测噪声挂钩,别让一次噪声就把有效测量判废;效应量落在计时分辨率附近时
   不得宣称方向——毫秒读数上的 1 毫秒差异是计时精度的产物,不是真实效应;
4. **不同机器、不同作业的数字不做减法**(机器间差异实测可达 25%);
   **端到端倍数必须带测量机器与日期**——同一个交付物在两台机器上实测差 24%,
   不带机器名的倍数没有意义;**交付物或环境一变,对外主打的数字必须复测**,过期数字引用时标注日期;
5. **结果是 0 或两边完全一样:先怀疑测量脚本坏了**再下结论
   (我们测量脚本出 bug 的频率,远高于被测系统出奇迹的频率);
6. **测量前先声明预期方向**;剂量(注入的改动量/负载量)加大,效应也应随之加大,是最硬的证据形态之一;
7. 结论出来立刻写盘落档;没落档的结论等于不存在;
8. **重试纪律**:同一原因失败两次不再重试;新的独立原因允许再投一次;
   单个检查点的作业投递封顶三次,超限就标"无法判断"收尾——无限重试是自欺。
   环境性中断(被抢占、为止损主动取消)不计入失败次数,但每次重投必须换对策
   (裁剪负载、换节点),原样重投不算对策;被中断作业的数据按段裁决:
   **中断点之前内部自洽闭合的段可用,之后的一律不用**,且中断事实写进证据;
9. **独占要自己见证**:调度器的"空闲"标签不可信(实测有节点标着空闲却常驻高负载);
   测速期间全程采样机器负载、把峰值写进结果,超过阈值,本轮就不出数;
10. **重建可复现是零成本强证据**:诊断或复测涉及重新构建时,两次独立构建的产物
    哈希逐字节相同,列为标准动作——它证明两轮测的是同一个东西;
    同一产物的哈希应**贯穿全卡各门的引用**(一条哈希链贯穿所有门,证明测的始终是同一对象)。

### §10 检查点标准

1. 启用前先对**已知坏样本**报错(否则这个检查不可信);
2. 启用前在已知好样本+已知坏样本上各试跑一遍;看不见被测对象时报"无法判断",绝不默认通过;
3. **"没观察到失败"≠"证明不会失败"**——输出只有四种:
   通过 / 失败 / 无法判断 / 不适用(必须带一句理由);
4. 判据优先用**产物**(哈希、计数、内容)而非耗时;
   证据强度排序:行为见证 > 配置存在 > 文件存在;
5. **作业成功≠步骤成功**:调度系统里"完成、退出码 0"的作业照样可能内部失败——
   关键步骤各带自己的退出码、产物断言与日志内 DONE 标记,收尾清单对这类行要加注;
   相互独立的测量分类降级,不许一个失败拖死全部;
6. **探针认对象按内容不按名字**:用哈希/符号确认测的到底是哪个文件——
   文件名匹配会认错人(实测:按文件名匹配认不出改了名的坏样本库,反而误判成环境变量没生效)。

### §11 加速成因三分法 → 交付形态

每个提速点先归因,归因决定怎么发:

| 成因 | 例子 | 交付形态 |
|---|---|---|
| **1 前人漏掉的**(须有上游注释/提交佐证) | 串行扫描本可并行 | 可进默认安装 + 给上游提补丁 |
| **2 有意的取舍,向速度重新偏置** | 压缩率换速度 | **绝不默认**;用户主动选装(opt-in),代价说明写进包里,并有检查确保说明存在 |
| **3 通用保守值 → 高性能机特化** | 线程数、缓存大小 | 按机器档次分包(或用元包——只声明依赖的空包)+ 硬件门槛检查 + 写明"不适用于谁" |

"上游意图"以上游源码的默认值或文档为证(例:上游 `CFLAGS ?= -O3` 被发行版默认顶掉
= 第 1 类;发行版明知而改 = 第 2 类)。归类证据写进卡。

第 3 类有一种**自分派特化**交付形态:函数级多版本(编译器的 `target_clones` 机制:同一函数编出多个硬件档的版本,运行时自动选)把硬件分档、
门槛检查与回退全部收进二进制内部——单一 .deb,不需要按硬件拆多个包或多份库文件,可进默认安装。
启用条件:双向硬件行为见证缺一不可(新 ISA 机实证走特化分支;旧 ISA(可用模拟器)实证
走默认分支、无非法指令、输出与原版逐字节同),且符号面零变化(§3 二进制 ABI 线)。

### §12 交付与安全

- **只发真正的 .deb**,签名 APT 源分发,一条命令安装;
- **版本号规则**:重建包统一用 `a0turboN` 形式后缀——实测(10 个抽样包):0 个会挡住官方安全更新、
  10 个都能覆盖官方基础版本。它的真正价值是**"无需逐包判断"**:`+turboN` 只在部分包上
  会挡住官方 `ubuntu0.X` 形状的安全修复(抽样 10 个包里 6 个会挡,风险高),逐包判断成本高且易错,
  统一 `a0turboN` 一劳永逸。**加速包绝不能挡住安全更新**;
- **替换方式三档**:整包原位重建(最优)> 官方文件转移机制(dpkg-divert)+ 一层委托包装
  (官方安全修复仍能生效)> 直接覆盖文件/无主替换(**禁止**);
- **动一个包 = 重建整个包族**(半重建会让严格版本依赖的兄弟包装不上,已实测);
- **已知问题登记册**:安全/兼容问题不要求当场全修,但必须登记。
  格式条款:**单一文件、编号 KI-N 递增、只追加不删除**(关闭只改状态),
  每项五栏——问题 / 证据 / 防再犯的检查 / 统一修复环节 / 状态;
  **未关闭的问题,发布材料必须原文列出**;关闭须引用证据(作业号/检查输出);
- **签名 APT 源不只是分发渠道,也是可测性前置**:配置线的"卸载→重装"循环、
  升级线的真实覆盖测试都要求包能重新获取——源没上线时这些检查只能部分做,必须如实标注;
- **上游回馈**:属于上游缺陷的修复整理成可直接提交的补丁;只有仓库所有者署名提交。

**发布关卡(全绿才发)**:端到端+正确性证据齐 + §3 八条兼容检查全过(含坏样本验证)+
登记册所有问题要么已关、要么已在发布材料原文列出。

### §13 共享环境使用规范

- 任何共享节点**禁写 `/tmp`**(曾把共享存储写满触发告警);
  临时文件只写沙箱内部或指定暂存区,退出时清理,收尾做"残留=0"复核
  (任务被强制中断时清理钩子不会跑,所以必须复核);
- 集群作业只投低优先级回收队列、排除登录/关键节点;投前先估核数与内存;
- 同一时刻只允许一个执行者写共享镜像/基线目录;基线引用一律用哈希或带后缀文件名;
- 容器/受限环境里 `nproc` 不等于物理核数,资源判断读 `/proc/cpuinfo`;
- 脚本投递前先过语法检查——一个引号错误就能烧掉整个作业。

### §14 协作与可追溯

- **对外每个数字必须走通:报告 → 账本 → 实验笔记 → 作业编号/原始输出**;断链不得对外;
- 账本(项目的结论日志)是数字唯一权威,只追加不改写;
- 跨任务的事实(基线更换、判据更新、坑)写进共享事实文档的编号附录并广播,
  开工先读;**追加附录前先查文件当前最大编号、取最大+1**;发现编号撞车不改旧文,
  新开一条勘误附录记录映射(多写者并行追加时撞号实测发生过);转述他人卡上的状态时只说自己核实过的内容,记不准就注明"以卡内记录为准",不要凭记忆复述具体数值;
- **命名**:项目/产品名 Linux Development Agent;新制品用 `lda-` 前缀;
  历史路径/包名/证据引文保持原值以保回溯链,下个发布周期统一迁移。

---

**References**:[Argus](https://github.com/lbx154/Argus) · arXiv:2608.05144 · argusbot.cn
