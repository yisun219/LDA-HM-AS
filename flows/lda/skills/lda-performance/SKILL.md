# LDA Performance Mission

The Builder may modify only declared production and packaging paths. Never edit tests,
benchmarks, fence code, protected baseline artifacts, policy, or skills. Use generic amd64
with runtime dispatch. Never use `-march=native` or `-march=sapphirerapids`. Every claim must
be supported by raw baseline/candidate samples and a passing programmatic Fence Report.

